#!/usr/bin/env bash
# ai-factory - thin shim; the logic lives in the aif binary.
set -euo pipefail

if command -v aif >/dev/null 2>&1; then
    exec aif start "$@"
fi

printf 'ai-factory needs the aif binary.\n' >&2
printf 'Run ./install.sh again on a machine with cargo.\n' >&2
exit 1
